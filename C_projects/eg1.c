#include <stdio.h> // studio.h imports all functions with in it
// 1_basics_structure
int main() { // main is function and returns integer 
   printf("\n Hello, World!"); // printf is the funcion that will print the comment
   // return 0; // we can return zero

      // there are keywords like 'return','int' can not be changed to other values.
      // the c program is a case sensetive language 

// 2_variables and datatypes 
      // variable_name = value
      // it shouldn't contain any reserved key word in the variable value
      // data types are char,float,double,int
 
   int a;
   int b;
   a = 10;
   b = 12;

   printf(" a + b = %d\n ",a+b);
   return 0;

}











